<%= form_for(fa_question) do |f| %>
  <% if fa_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fa_question.errors.count, "error") %> prohibited this fa_question from being saved:</h2>

      <ul>
      <% fa_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_area :question %>
  </div>

  <div class="field">
    <%= f.label :answer %>
    <%= f.text_area :answer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
